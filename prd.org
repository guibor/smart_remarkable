#+title: Smart Remarkable Paper Pro Deployment

* Requirements

- Run Smart Remarkable on a Developer Mode reMarkable Paper Pro with firmware 3.28.
- Keep the stock =xochitl= notebook application running; do not take over the display.
- Install runtime files only under encrypted =/home= and do not add a boot service.
- Send selected-note requests to the user's canonical OpenClaw =main= session rather than calling an LLM provider with an API key.
- Reuse the exact =agent:main:main= session with =whatsapp= message context so Smart Remarkable sees the same server-side history, memories, and agent tools as the user's WhatsApp conversation.
- Keep all conversation history and memory on the OpenClaw server; beyond the requested answer typed into the stock note, do not retain separate screenshots, output files, or a transcript on the tablet.
- Keep the OpenClaw Gateway private and never expose its operator endpoint or credential to the tablet or public web. The server-side bridge may authenticate to the loopback Gateway; the tablet reaches only the narrow loopback bridge through a one-shot SSH tunnel owned by a dedicated password-locked Unix account. That account must permit only client-local TCP forwarding to literal =127.0.0.1:18792= and deny remote TCP forwarding, StreamLocal forwarding, shell/exec/subsystem sessions, PTY, agent/X11 forwarding, tunnels, and user rc.
- Let the user lasso handwriting with the stock selection tool, submit through the configured explicit or pen gesture, and receive a stock text-box answer below it.
- Provide one reversible ReMagic/AppLoad tile that toggles the configured bounded worker. It must not create separate tiles for the three modes.
- Use the repository's existing =draw_text= implementation for answers; do not convert ordinary text into simulated pen strokes.
- Consume root-only volatile trigger files under =/run/smart-remarkable= even while the Paper Pro touch device is emitting continuous events, and remove them when the worker stops.
- Do not install the ABI-sensitive raw Xovi =llmbutton.so= LLM/Draw extension on firmware 3.28. A native-button candidate must use the firmware-specific QMLDiff/AppLoad path and add only the approved answer-here and agent actions, represented by exact stock reMarkable sparkle resources rather than text glyphs or custom assets.
- Refuse kernel-module loading by default and require the firmware-provided =/dev/uinput= device for this deployment.
- Keep Smart Remarkable and T.M.R. mutually exclusive, give Smart its own tunnel port, and stop Smart whenever stock =xochitl= stops.
- Keep Smart Remarkable's unauthenticated web server and debug/request logging disabled.
- Preserve a recovery path over key-authenticated Wi-Fi SSH.
- Resolve the Paper Pro framebuffer across every contiguous =/dev/dri/card0= mapping group and detached terminal frame allocation after each =xochitl= restart; reject malformed, looping, overflowing, or incompletely readable candidates instead of assuming the final graphics mapping is the display.
- Show exactly one Smart Remarkable entry in AppLoad; keep deployment stages, failed candidates, and timestamped rollback bundles in a root-only recovery directory outside AppLoad's scanned application tree.
- Make installer provenance independent of Git tracking state: deterministically hash every regular file placed in the AppLoad archive, verify the exact extracted file set and the separately transferred device installer before mutation, and retain the manifest plus its own hash in root-only recovery metadata. Untracked staged sources must receive the same coverage as tracked sources.
- Provide exactly three configurable runtime modes without adding duplicate AppLoad tiles:
  - =once=: accept one valid selection and exit.
  - =session-hold=: remain active for a bounded session; submit only when the user closes a native lasso, keeps the pen tip touching nearly still for the configured hold duration, and then lifts.
  - =session-auto=: remain active for a bounded session and submit every completed native lasso, with an explicit privacy warning because ordinary selection edits are also eligible.
- Keep the mode and non-secret session settings in a root-owned configuration file separate from the OpenClaw credential. The single AppLoad tile must toggle the current session on/off.
- In session modes, admit at most one request at a time, drop pen contacts that began while processing, suppress a still-active selection from duplicate submission, and clear cropped image/model scratch state after every success, error, or no-submit run.
- Allow optional native answer-here and agent selection-menu actions as the preferred explicit triggers only after a firmware-specific, read-only compatibility probe succeeds. They must use the stock notebook-with-sparkles and sparkles icons with no user-visible implementation labels. The QML patch may ask AppLoad to signal a trigger but must never contain credentials, call OpenClaw, load a kernel module, stop =xochitl=, or start at boot.
- Place two explicit native buttons immediately after the stock Copy action, in the order answer-here then agent. The first must use the firmware's stock sparkling-notebook resource and the second its stock sparkles resource. A tap must show immediate selected-state feedback, reject repeated taps while pending, and pass one canonical v3 descriptor containing response mode, trusted =ink=/=image=/=mixed= kind, stable =normal=/=rot180= QML orientation, fixed-point selection-root bounds, exact document UUID, page id/index, current page-view bounds, and capture time. The exact-firmware =DeviceSceneView= patch must explicitly supply that document/page identity to =SceneSelectionHandler=; parent-chain discovery is forbidden. After strict validation, AppLoad must read 32 kernel-random bytes through the exact device-proven =/usr/bin/hexdump -n 32 -v -e '1/1 "%02x"' /dev/urandom= interface, accept only exactly 64 lowercase hexadecimal characters, add that fresh 256-bit nonce, publish a root-only busy marker before exactly one trigger, and reject stale, non-canonical, unknown-transform, navigation-mismatched, degenerate, or out-of-bounds input. The installer must prove that nonce capability on the exact live firmware before mutation. Rust must request the prepare phase; QML must immediately re-read the live kind/orientation/bounds and document/page identity, reversibly bind only =selectionRoot.controlsAreVisible=false= on an exact match, and return a snapshot that AppLoad binds to that nonce. Rust must consume that exact acknowledgement before taking an explicit-orientation in-memory capture and resolving the visible reMarkable document name through a bounded, no-follow, root-owned metadata read. After preparation, QML must revalidate the complete snapshot again, call =SceneSelectionHandler.close()=, and acknowledge only after the handler is hidden; Rust must consume the same-nonce close acknowledgement before remote work. Pre-close failures must restore or preserve the visible selection. If stock close succeeds but acknowledgement publication or consumption fails, remote work must abort without altering underlying content, although Smart cannot recreate the closed handler. Neither button may invoke Copy, Cut, Delete, movement, or simulated touch coordinates. The prior canonical v2 form remains only a strict app-first migration/rollback protocol and must never be silently interpreted as v3 context.
- Commit the button's selected and pending state before launch, yield one QML event-loop turn so feedback can paint, and then revalidate the exact mode, snapshot, and visible selection. Dynamically import, parent, and cache exactly one =AppLoadLibrary= per selection handler before a tap when possible; reuse it for descriptor, prepare-ack, and close-ack launches. Each phase must call =launchExternal("external::smart-remarkable", -1, [argument], ({}))= exactly once, accept only a positive returned PID, and clear pending state on a non-positive result or exception. QML and the launcher must emit content-free =SR_WAND= stage diagnostics sufficient to locate a failed boundary without including the descriptor, geometry, nonce, recognized text, image, or selected content. Do not use the process-wide =AppLoadLauncher= signal, create or maximize an AppLoad window, add a persistent/boot service, or wait for the worker, tunnel, or remote request in QML. AppLoad's process-start check is the only synchronous boundary on the stock UI thread; all longer work must continue in the transient child.
- Before upload, transiently prepare every selected crop according to its trusted kind. =ink= retains the gray-overlay-to-white normalization and bounded aspect-preserving enlargement. =image= and =mixed= preserve RGB, tonal detail, and readable light content while applying only bounded aspect-preserving enlargement. Fingerprinting and model submission must bind the kind and use the prepared PNG; neither the source nor prepared crop may be written to a tablet file. Unknown, malformed, stale, degenerate, or out-of-bounds descriptors must fail closed rather than fall back to an accidental gray region.
- For every v3 explicit-button turn, create one immutable request-context bundle from the same prepared framebuffer read: the kind-prepared exact lasso crop is =selected_region= and remains the primary user input; the visually faithful =current_page_view= is supporting context from that same instant; and bounded metadata explains the reMarkable document display name, page id/index/number, selection role, page-image role, and whether the page image is a complete visible page or only the current zoomed/panned viewport. Never auto-zoom, navigate, export, or take a second screenshot to manufacture a full page. Neither PNG nor the raw document-display-name request field may be logged, written to a tablet outbox, or persisted as request data in the bridge journal; clear both images and metadata from request scratch state after every terminal path. The title is intentionally part of the canonical OpenClaw turn, and a cached safe response may naturally mention it.
- Keep the stock notebook responsive after local capture while the existing bounded worker continues the canonical OpenClaw turn, tool work, receipt-checked WhatsApp delivery, and optional answer write-back.
  This is asynchronous user interaction, not concurrent canonical turns: the launcher must publish busy before the trigger, the worker must continue to admit at most one request until the complete terminal result and cleanup, internal admission must reopen before the exact busy generation is consumed last, and later button taps during that interval must fail rather than queue. A stale or invalid consumed trigger may release =busy= only when the root-owned marker is an exact byte match for that generation; a mismatch must terminate the worker so runner cleanup fails closed.
  Local =ready= may be published only after every fallible engine/tool setup step and must be owned by the trigger-listener future so normal return, error, panic unwind, or abort removes it. Listener death must win over simultaneous request completion; if it occurs during processing, main must cancel and boundedly settle or abort the processing task before emitting the idempotent prepared-selection restore chord and exiting.
  Because the user may navigate or edit after the local marquee closes, =write_back= must retain the original prepared full-page framebuffer bytes in memory and bind the first post-close view back to that exact original before remote work; a page reached during close must never become a new baseline. Failure to rebind must continue canonical OpenClaw/WhatsApp delivery with local insertion suppressed. Every production insertion must require this exact v3 guard; legacy-v1, v2 migration, and pen-lasso routes must remain local-write-suppressed.
  It must then visibly verify the firmware-pinned stock Text selection, inspect, close, and verify the palette, choose a placement clear of stock chrome, permit only a disjoint pinned closed-toolbar delta plus a non-empty caret-like vertical delta in a narrow 16-by-64 placement region, and suppress typing on every screenshot, tool, target, or geometry failure. Palette cleanup must never blindly toggle an unknown state.
  Before placement proof and during actual typing it must observe physical touch and pen contacts non-exclusively so =xochitl= always receives complete gestures. The monitor must query current pen-button state and every multitouch tracking slot before output, check for new interaction before every key, and abort the remaining response on interaction. Body-style and every shifted or unshifted character must use one balanced evdev batch with no modifier left across a guard boundary.
  It must reject the entire local insertion before its first key unless every character is representable and the response fits the tablet's 2,048-byte, 600-key, approximately 6.5-second emission budget. WhatsApp remains canonical for every suppressed insertion. This is still framebuffer identity rather than a semantic document/page id, and the caret signature/timing remain firmware-calibrated physical promotion gates.
- Make asynchronous local close resilient to a temporarily absent restricted SSH tunnel without creating a tablet outbox. Serialize the complete explicit-button inspect/stop/start/admit transition with an exact-device-proven, root-only, auto-releasing, bounded-wait fd lock separate from runner cleanup; never stop a unit that owns a busy generation; tolerate a =RuntimeMaxSec= stop race only after proving the unit is already inactive or unloaded; give every accepted explicit button a fresh transient lifetime of at least one hour; and wait boundedly for the stopped =--collect= unit to become =LoadState=not-found= before reusing its fixed name. The installer must prove on the exact live firmware that =flock= works, pin =Dropbear v2025.88= from =ssh -V=, and verify native =-K= plus every required extended option from non-networking help output before mutation; =ssh -G= and unsupported OpenSSH-only options are forbidden. On initial or later SSH failure, remove remote readiness, reap only the tunnel child, and reconnect while retaining at most one request-context bundle—selection PNG, current-page-view PNG, and bounded metadata—in Rust memory through one bounded fifteen-minute bridge-readiness window. SSH setup is bounded by parent-owned health probes and established transport by Dropbear's native three-miss =-K 30= keepalive. If the OpenClaw HTTP connection or response body is interrupted, or the bridge returns pre-acceptance 502, 503, or 504, retry only that bridge request inside a separate overall fifteen-minute transport window with the exact same serialized body and request id, emit remote acceptance at most once, and rely on the bridge's live or completed-durable idempotency response. Redirects, non-200 success statuses, and 4xx responses remain terminal; in particular, never forward the context bundle/token to a redirect or convert an incomplete reservation left after a bridge-process restart into a second turn. The two consecutive windows may therefore retain the one bundle in memory for roughly thirty minutes in the worst case, still bounded by the fresh one-hour unit. Do not apply this retry to direct provider requests. Reboot, =xochitl= stop, deliberate service stop, or process kill may still lose the in-memory request; surviving those boundaries would require persistent selected content and is outside the no-retained-context requirement.
- Treat the two buttons as response destinations, independently of the existing =once=, =session-hold=, and =session-auto= trigger modes:
  - answer-here / =write_back=: deliver a "reading" acknowledgement followed by one WhatsApp message containing both OpenClaw's literal transcription and its answer, then insert only that same answer as stock text in the notebook.
  - agent / =whatsapp_only=: deliver the same acknowledgement and transcription-plus-answer WhatsApp message without selecting a tablet text tool, typing, drawing, or otherwise inserting a response into the notebook.
- Submit both button modes as one canonical OpenClaw agent turn through native Gateway =chat.send= with exactly two fixed-role PNG attachments: =remarkable-selection.png= is the selected primary input and =remarkable-current-page.png= is same-instant supporting context. The tablet request must carry the exact =selection-page-v1= header/body schema and bounded document/page metadata; the bridge must reject absent, reordered, duplicated, extra, oversized, or role-mismatched parts and fingerprint both images plus canonical metadata. Preserve exact =agent:main:main= session routing, the startup-validated =expectedSessionRoutingContract= of =per-sender|main|main=, =deliver=false=, disabled command interpretation, and a unique idempotency key. A scope, main-key, default-agent, context-version, plugin-capability, or origin-protocol change must fail at startup and again at admission rather than silently recanonicalizing or accepting a partial deployment. Deliver the acknowledgement and final to the server-owned canonical WhatsApp route through a narrow plugin-owned Gateway method that requires a native provider receipt and deliberately omits transcript mirroring. Do not infer delivery from the synthetic =x-openclaw-message-channel= HTTP header or from successful agent completion alone.
- Generate every tablet-attempt id in the exact =smart-remarkable-= namespace and accept only =smart-remarkable-[A-Za-z0-9][A-Za-z0-9._:-]{0,110}= throughout HTTP validation, durable request identity, provenance bind/clear receipts, and plugin delivery. A syntactically valid but non-namespaced run id must never reach journal reservation, host authority, =chat.send=, or WhatsApp delivery.
- Treat the OpenClaw plugin capability receipt as scoped to one authenticated Gateway connection generation, not as a process-lifetime startup fact. The initial listener, every =/health= response, and every new admission before journal reservation must require plugin =0.4.0=, origin-v4, ordered =selection-page-v1= input-context versions, ordered =selection=/=current_page= attachment roles, and the ordered selection-kind set for the current generation. A disconnect or authenticated reconnect invalidates readiness; generation-pinned RPCs must reject a connection change before or during the call. Journal schema v3 must bind the request fingerprint, both images, canonical metadata, response mode, selection kind, context version, and cached response without persisting either image or the raw document-name request field. Existing schema-v1 and schema-v2 journal reservations and completions are deliberate HTTP-409 barriers under schema v3: never replay, resubmit, delete, or free their capacity automatically. Promote origin-v4/plugin-0.4, response envelope v3, journal schema v3, and the matching bridge only as one quiesced guarded server transaction while the tablet buttons are inert.
- Before =chat.send=, bind protocol version, worker-generated request id, response mode, trusted selection kind, and preflight-captured transcript id through =operator.admin=-scoped bind and clear methods. Store the pending authority as a bounded, realm-neutral JSON string in OpenClaw's host-owned run context so the Gateway startup registry, active prompt hooks, and pinned tool registry share one source of truth. Because ordinary plugin API methods close after =register=, every late bind-side get/set/clear must go through the plugin's registered agent-event run-context adapter: keep the complete operation and value only in the originating plugin instance's bounded private map, emit only a fresh random operation id on the plugin-owned control stream, and let the synchronous host subscription callback perform =getRunContext=, =setRunContext=, or =clearRunContext=. A set or clear succeeds only after exact host read-back; absent or asynchronous callback delivery, missing host methods, an emission/receipt mismatch, capacity exhaustion, or any exception must fail closed. This adapter is valid only for the exactly pinned OpenClaw 2026.7.1 synchronous event lifecycle; an OpenClaw upgrade must remain blocked until the late-call guard and same-stack callback ordering are re-audited. A pending record must expire after twelve minutes, activation must require the exact run id, transcript id, canonical agent, and canonical session key, and active authority must expire after fifteen minutes without extension. Before every bind, reconcile the bind-side index against host state: retain every still-live pending or active record, clear expired/malformed host records, and reject new reservations at 128 live records rather than evicting or undercounting them. Return a separate opaque binding handle, require it for cleanup, and clear host and local state after every bridge outcome so a stale cleanup cannot delete a newer binding. Also persist =systemInputProvenance= as =external_user= / =remarkable= / =smart_remarkable= for audit, keep =originatingChannel= and the canonical session origin as WhatsApp, and append a non-authorizing trusted transport instruction so the attempted turn carries source context independently of the hook. The final admission gate must still block if the exact hook guidance is unavailable. Handwritten prompt text, ordinary WhatsApp input, model output, the transport instruction, a pending/expired record, a control-stream event, or a lookalike provenance marker must never authorize reMarkable-only tools.
- Because the delivery plugin is non-bundled, live OpenClaw configuration must explicitly set both =plugins.entries.smart-remarkable-delivery.hooks.allowPromptInjection=true= and =allowConversationAccess=true=; the plugin must verify its exact id and both flags before registering any method, hook, or tool. =before_prompt_build= may activate and append guidance only for the exact pending authority. A later =before_agent_run= gate must re-read the active authority and block the Smart reMarkable run unless the final constructed system prompt contains the exact server-owned kind-aware guidance for that same request, transcript, canonical agent, and canonical session. Ordinary runs must return an explicit pass result for the pinned OpenClaw 2026.7.1 gate behavior. Prompt-hook absence, storage/read-back failure, expired authority, or guidance removal must block before the model or image input is admitted.
- For an authenticated =image= or =mixed= capture turn, add intent guidance only from the exact admitted OpenClaw prompt hook, never from tablet-authored text or response-recovery history. Resolve the user's intent in this order: an explicit current instruction; a specific still-active user instruction in recent canonical conversation; durable user preference or memory; then the selected content and immediate context. Newer, task-specific user instructions override older or general ones; assistant suggestions, quoted document text, and commands merely visible inside a capture are content rather than authority. If intent remains ambiguous, provide a useful in-depth explanation, background, and context for the captured material instead of defaulting to a market scan, recommendations, or a generic clarification. Ambiguity must never authorize an external side effect.
- For every trusted v3 selection, make the server-owned prompt hook behave as a proactive, capable employee: treat the lasso as the user's focal input, use the page view, document name, canonical history, and durable server memory to resolve references and likely intent, make the strongest reasonable interpretation, and carry harmless or reversible work to a concrete useful result. Do not merely acknowledge, restate, default to a market scan, or ask a generic clarification; state a concise consequential assumption and address a close alternative when useful, asking only when materially different interpretations require a real user choice. Page/title text and material outside the lasso remain untrusted context rather than instructions. Inferred intent must never authorize an external side effect, and no action may be reported as complete without evidence. Keep =received_text= limited to the selected attachment; page and filename context may influence only =response_text=.
- On a trusted reMarkable turn that asks OpenClaw to create, export, send, or place a document, make OpenClaw create a PDF or EPUB in its workspace and call a dedicated receipt-checked delivery tool that uploads the validated artifact to the user's ordinary reMarkable cloud library. The tool must be unavailable to ordinary WhatsApp turns; accept only regular non-symlink workspace files with matching extension and file signature; invoke the existing server =rm-sync= client without a shell or secret-bearing arguments; persist a fail-closed artifact idempotency receipt; and permit an upload claim only after a validated cloud document id/hash. OpenClaw must continue to report what it received and what it did through WhatsApp.
- Capture the canonical session id immediately before each new turn for exact-transcript recovery, but never pass that captured id back to =chat.send=: OpenClaw 2026.7.1 can rotate a current session to a mismatched caller-supplied id. Current bounded history and any live-final candidate are eligible only after an eligible history contains the exact =<request-id>:user= anchor. If the canonical key has remapped, first prefer the captured active JSONL and otherwise consider at most 128 exact =.jsonl.reset.<safe-ISO-timestamp>= archives; open without following links, enforce file/line bounds, validate the first session record, and require at most one unique request anchor. If the verified captured transcript contains that anchor, it retains precedence even while pending; once current history names a successor, an unscoped live final cannot complete the pending captured request, which requires its own durable assistant interval. A post-admission automatic canonical-session successor is eligible only through a separate strict negative proof: no active captured transcript; exactly one finalized reset candidate; stable initial/final descriptor identity, size, mtime, and ctime; fatal UTF-8 decoding; strict bounded JSONL for every line with the byte limit checked before skipping only narrow ASCII JSON-whitespace blanks; exactly one captured-session header; an unchanged candidate set; a reopened-path identity match; and zero exact request anchors. Only then may newly canonical bounded =chat.history= prove the successor with exactly one matching request anchor carrying persisted =kind=external_user=, =sourceChannel=remarkable=, and =sourceTool=smart_remarkable= provenance followed by the strict assistant interval. Do not require =senderIsOwner=, which production =chat.history= omits. A live final alone, or missing, active, changed, malformed, oversized, invalid-UTF-8, mismatched, ambiguous, or anchored captured evidence, must not authorize successor-session history. Never open another session's transcript file.
- For every accepted reMarkable turn, make WhatsApp visibly acknowledge that OpenClaw is reading the selection. The one canonical assistant final must use the server-owned strict ={"received_text":"...","response_text":"..."}= envelope. For =ink=, =received_text= is a literal transcription. For =image= or =mixed=, it is a faithful account of what was selected: preserve legible visible text and add only the concise visual identification needed to say what OpenClaw actually received, without putting inferred intent or the answer in that field. The bridge must reject malformed, empty, oversized, or control-character-bearing envelopes; render the successful WhatsApp final atomically as =I read:= plus the quoted received field followed by the answer; and render =[unclear]= as an explicit inability to read rather than a guess. Do not mutate the canonical session's persistent verbose setting or expose raw commands, model reasoning, or tool outputs.
- Keep the full OpenClaw operator credential and WhatsApp delivery target on the server. Expose only a loopback-bound, narrowly authenticated reMarkable bridge through the existing forwarding-only SSH path, and store only its narrow credential on the tablet.
- Return only =response_text= to the tablet, and only for the first successful =write_back=; the WhatsApp-only =I read:= prefix must never be inserted into the notebook. A request is complete only when native OpenClaw delivery reports both the reading acknowledgement and the atomic transcription-plus-answer final as successful WhatsApp outcomes. Persistently reserve each request id before =chat.send= so a bridge/Gateway restart cannot create another canonical agent turn; a surviving incomplete reservation must fail closed without resubmission. Mark cached responses as replays, and require the tablet to suppress =draw_text= for every replay so duplicate client retries cannot create another agent turn, acknowledgement, final WhatsApp delivery, or notebook insertion. Ambiguous outcomes may omit a result but must never claim or manufacture exactly-once delivery.
- The native-button deployment must be fail-open for the stock UI: exact Qt symbols and object names are required, missing/mismatched prerequisites must disable injection, and a bounded watchdog plus preserved rollback must remove the extension if =xochitl= fails to remain healthy. Do not remount or write the system partition.
- Permit coexistence with other installed QMLDiff hacks only when every co-resident =.qmd= has an explicitly allowlisted filename, SHA-256, root ownership, and mode 0644. Missing, changed, symlinked, or additional =.qmd=, =.qrr=, or =.rcc= files must abort the button transaction before mutation.
- Bind every functional 3.28.0.164 QMD promotion to one reviewed offline artifact contract containing the exact new-protocol worker, AppLoad launcher, run-armed runner, selection-protocol helper, source QMD, and compiled QMD hashes. The application installer must bundle and verify that same contract and may coexist only with an absent Smart QMD, the exact approved legacy-functional QMD, the exact =v2-migration-functional= QMD, the exact new inert QMD, or the exact new functional QMD. Both device installers must share one deployment lock. Preserve the strict app-first migration adapter and require intentional rollback to restore an approved QMD state before restoring an older app. Ordinarily, revising a functional hash must first use the still-pinned old contract and qualification marker to move the deployed functional QMD to inert. The resource-expanding v3 exception is only the explicitly documented no-taps sequence below: install its backward-compatible app beside the exact v2 functional QMD, then use the new contract to move immediately to inert before changing the server.
- Keep =session-hold= usable without the injected button as the safe fallback. Holding means: draw and close the lasso while the pen is touching, keep its endpoint within a small movement radius for the configured duration (initially 800 ms), then lift; an ordinary immediate lift must remain a normal reMarkable selection and must not submit.
- Separate local-capture readiness from remote-bridge readiness. The AppLoad launcher may publish a validated explicit descriptor only while the exact local worker advertises a fresh admission generation and is not busy; it must not wait for an SSH tunnel or bridge health before the immutable crop/close handshake. The runner must establish and continuously supervise the private tunnel in parallel, publish a distinct remote-ready marker only after the forwarded bridge's capability-backed health probe succeeds, remove that marker on tunnel failure, and make the worker wait for it before remote submission. A tunnel failure may fail the already-local request but must never leave stale local readiness, admit queued taps, or recreate/change the closed stock selection.
- Firmware updates may deliberately return the tablet to stock operation. The
  supported workflow does not need to persist Xovi across an update; instead,
  regenerate the exact-firmware QMD and allowlist, reinstall through the
  documented bounded recipe, and preserve the existing =/home=-backed Smart
  Remarkable settings and worker bundle.

* Requirements that changed

- The =0.8.0-openclaw= deployment candidate originally pinned BetterTOC's
  reviewed 3.28.0.164 r2 QMD. Before the no-taps migration, BetterTOC had
  already been upgraded independently to the reviewed r3 QMD at SHA-256
  =903e40e97d6d48923c6f76f77b5ff7f20ac156ee2b01ee266f51e4487dfe68fb=.
  The canary correctly failed before =ARMED= and made no tablet change. The
  coexistence contract now preserves and pins that exact r3 artifact; it must
  still reject either BetterTOC revision when the filename, ownership, mode,
  or SHA-256 differs from the one current contract value.

- The QMD canary controller originally admitted a compiled artifact after =qmldiff check-compatibility= against the live hashtable. A later parser failure showed that resource-hash compatibility alone is insufficient. The changed requirement makes the exact extracted stock =SceneSelectionHandler.qml= and =DeviceSceneView.qml= regular non-symlink inputs and requires a real host-side =apply-diffs= into a private temporary directory, with both patched outputs regular, non-symlink, nonempty, and accepted by =qmlformat --ignore-settings= before any remote mutation.

- The accepted =0.7.3-openclaw= path sent only the prepared lasso crop and a generic selection prompt. The user now requires the selection, same-instant current page view, and human-readable reMarkable document name with explicit role metadata so canonical OpenClaw can understand references and surrounding context. The changed requirement introduces a nonce-bound v3 document/page snapshot, safe read-only display-name resolution, a strict two-image =selection-page-v1= bridge contract, and authoritative proactive best-effort guidance. It does not turn surrounding page text into user authority, auto-zoom or navigate the tablet, retain a screenshot outbox, or weaken explicit authorization for external side effects.

- The first working deployment called OpenAI directly with an API key, then moved to an isolated =openclaw/default= session identified as =smart-remarkable-rmpp=. The supported backend is now =openclaw/main= with exact =agent:main:main= and =whatsapp= headers through the private OpenClaw Gateway; no provider API key or separate OpenAI =user= identity may be used by the tablet workflow.
- The first trial used a persistent corner-trigger Select Mode process. Paper Pro touch events caused repeated and tiny selections, so it was replaced by one-shot native-selection activation. Trigger state subsequently moved from shared =/tmp/llm_button_trigger= to root-only volatile =/run/smart-remarkable/llm_button_trigger= as part of the bounded three-mode design.
- The upstream trigger-file poll waited on a timer that was starved by continuous Paper Pro touch events. File consumption now runs before each event wait.
- The first successful native-selection response used =selection.json= and =draw_answer=, which rasterized text and retraced it as visibly distorted pen strokes. It was first changed to =selection_print.json=; the current OpenClaw workflow uses =selection_openclaw.json= with the same existing stock =draw_text= output path.
- Riddle's full-display takeover was originally replaced by Smart Remarkable's stock-UI integration. T.M.R. is now retained as a separately launchable diary, while Smart Remarkable coexists as a mutually exclusive, stock-UI assistant with one-shot and bounded-session modes.
- The first current-device validation inherited goMarkableStream's assumption that the final =/dev/dri/card0= mapping group belongs to the Paper Pro framebuffer. A later Riddle-driven =xochitl= restart changed allocation order: the final group then began with an invalid zero header while the valid frame chain was in the middle group. The supported locator must now probe all mapping groups and fail closed if none yields a complete frame.
- A subsequent =xochitl= layout split the terminal =0xd73000=-byte frame allocation away from both =card0= groups: the graphics-linked chain ended in an empty slot, while a standalone anonymous mapping began with the expected terminal =0xd73002= header. The locator must therefore validate large detached anonymous allocations through the same bounded header and full-frame checks; adjacency to =card0= is not a supported invariant.
- The first transactional installer preserved rollback application directories directly below AppLoad's scanned root. AppLoad correctly treated their copied manifests as extra applications, so three Smart Remarkable tiles appeared. Rollbacks must now remain preserved under =/home/root/.smart-remarkable-recovery=, which AppLoad does not enumerate.
- The armed AppLoad trial originally required a four-finger tap after lassoing. Live logs proved that Smart recognized the gesture, but xochitl cleared the gray marquee before the subsequent capture. The supported workflow now triggers from the lasso pen-up itself, after a short stock-UI render delay, so no second screen contact can dismiss the selection.
- The first pen-up design would have treated any pen tap after arming as the one-shot request and exited when no marquee existed. A no-marquee pen-up is now ignored, keeping the worker armed for the actual native lasso.
- The supported firmware-3.28 deployment originally prohibited any injected button because the raw Xovi =llmbutton.so= uses private Qt ABI and its development history includes =xochitl= crashes. The user has now explicitly requested a native button as the preferred interaction. The raw extension remains prohibited; the changed requirement permits only an exact-firmware QMLDiff/AppLoad candidate, staged through an inert canary, live compatibility checks, bounded monitoring, and automatic rollback.
- The original AppLoad workflow exited after every answer. It now needs configurable finite sessions so multiple selections can be handled from one AppLoad activation; one-shot behavior remains available.
- The original launcher used =smart-remarkable-once.service= with a fixed ten-minute limit. The single tile now targets =smart-remarkable-session.service= and chooses the bounded lifetime from the strict non-secret mode settings.
- The first functional firmware-specific button was inserted before Cut and left the lasso marquee/menu visible until later tool execution, so a successful tap looked unacknowledged and invited repeated taps. The revised requirement puts =LLM= directly after Copy, highlights it immediately, and closes only the already-captured selection through stock QML state—without copying or deleting the selected ink.
- The first button safety allowlist assumed Smart Remarkable was the only QMLDiff patch. A later inventory found ReMagic's =penLayerMemory-beta-3.28.0.163.qmd= installed while =quickSettingsTimer-beta-3.28.0.163.qmd= was absent. The current device-specific policy therefore requires the exact Pen Layer Memory file and hash, permits Quick Settings Timer only at its exact reviewed name and hash if it is installed later, and continues to reject every unrecognized QML artifact.
- The first OpenClaw integration used the OpenAI-compatible =/v1/chat/completions= endpoint and set =x-openclaw-message-channel: whatsapp=. That header supplies synthetic channel context but the endpoint explicitly does not deliver its result to WhatsApp; the tablet consumed the HTTP final and always called =draw_text=. The supported design now uses native Gateway =chat.send= for the canonical agent turn, a separate receipt-checked no-mirror WhatsApp delivery method, and distinct =write_back= and =whatsapp_only= destinations.
- The first native-menu design exposed only =LLM=. The user has now selected two explicit adjacent buttons rather than a hidden long-press or an AppLoad mode switch: =LLM= delivers to WhatsApp and writes the same final into the notebook, while =Send= delivers to WhatsApp only.
- The live ReMagic set changed again after the first coexistence review. The exact-device allowlist must now include the currently installed and hash-pinned Better TOC, Better TOC Collapse, Gestik, Ghostbuster, Pen Layer Memory, Quick Settings Timer, and TOC From Selection QMDs, while continuing to reject every additional, missing, changed, symlinked, wrongly owned, or wrongly moded QML artifact.
- The first native-Gateway bridge plan prepended =/verbose on= to each tablet turn to obtain tool-start messages. OpenClaw 2026.7.1 permits this admin-scoped directive to persist =verboseLevel= on =agent:main:main=, which would alter later WhatsApp behavior. The supported bridge instead disables command interpretation, sends one explicit delivery-checked "reading and working" WhatsApp acknowledgement, and sends the exact final separately without changing canonical session settings.
- The next bridge draft used OpenClaw core Gateway =send= for the acknowledgement and final. In OpenClaw 2026.7.1 that method derives the direct session and mirrors each outbound message into the canonical transcript, which would duplicate the agent's final answer in history. The supported design now keeps =chat.send deliver=false= as the sole transcript owner and uses a tightly scoped plugin Gateway method around the public durable outbound channel API with no =mirror= or session context.
- The first bridge service draft was a systemd user unit with =ProtectHome= and =ProtectSystem=. The live server runs systemd 249, where those protections are not enforced by the user manager. The production bridge is therefore a system service that immediately drops to =User=mdf=, receives one private writable =StateDirectory=, and keeps the rest of the home and system filesystem read-only; OpenClaw's Gateway remains an independent user service.
- The first AppLoad installer provenance recorded =git_head= plus =git diff --binary=, which omitted untracked deployment inputs and staged index changes even though several such files were bundled or executed. Git metadata is now contextual only: the supported installer hashes the final bytes of the complete staged file set, verifies the separately transferred device installer, and preserves the manifest trust chain in recovery metadata.
- The first two-button tunnel plan reused the server's ordinary =mdf= account and attempted to express a local-forward-only policy in one =authorized_keys= line. OpenSSH 8.9 uses one per-key flag for local TCP, remote TCP, and StreamLocal forwarding, so =restrict,port-forwarding,permitopen= cannot close the reverse and Unix-socket paths. The supported tunnel now uses a dedicated password-locked Unix account plus a validated =Match User= policy with =AllowTcpForwarding local=, =AllowStreamLocalForwarding no=, exact =PermitOpen 127.0.0.1:18792=, =PermitListen none=, and no sessions or interactive features.
- The first live two-button build uploaded the marquee crop exactly as rendered, so real selections as small as 109×46 and 139×46 pixels reached OpenClaw with gray selection fill at very low resolution. It also sent only a generic "reading and working" acknowledgement and expected the ordinary assistant final to serve simultaneously as the WhatsApp message and notebook insertion. The revised requirement transiently normalizes and enlarges selected handwriting; requires the single canonical assistant final to separate literal =received_text= from =response_text=; makes the WhatsApp final quote what OpenClaw read; and keeps only =response_text= eligible for notebook insertion.
- The first two-button presentation used literal =LLM= and =Send= labels. The current interaction uses the firmware's own sparkling-notebook icon for answer-here and sparkles icon for send-to-agent, preserving the same two explicit destinations and after-Copy order without fragile custom artwork or ambiguous implementation jargon.
- The first bridge prompt merely said that the selection came from reMarkable. That was useful context but not an authorization boundary and gave document requests no reliable artifact destination. The current requirement binds trusted plugin-owned admission state before =chat.send=, records durable input provenance separately from WhatsApp reply routing, and gates a receipt-checked reMarkable-cloud document tool on the exact active admission.
- The first completion recovery followed only whatever transcript =agent:main:main= currently referenced; a later session remap made one successfully completed selection look like a timeout. The bridge now captures the pre-turn transcript id for bounded exact-file fallback while deliberately omitting unsafe =chat.send.sessionId= repinning.
- The first captured-transcript fallback assumed OpenClaw retained =<session-id>.jsonl=. A real reset renames it to =<session-id>.jsonl.reset.<timestamp>=, while a live final or replacement-session history could otherwise bypass exact transcript identity. Recovery now handles only bounded exact reset names with session-header/anchor validation, and reMarkable prompt/tool authority is bound to the captured transcript. The bridge also forwards the fixed routing contract so a configuration drift cannot silently change what =agent:main:main= means.
- The first reset-archive rule rejected every newly canonical history after a remap, even when =chat.send= itself created a post-admission automatic canonical-session successor and wrote the exact request plus valid assistant response there. That turned a completed OpenClaw turn into a bridge timeout until a later conversation prompted recovery. An initial repair treated the tolerant reader's no-anchor result as proof, but malformed pre-anchor lines, non-fatal UTF-8, a growing active transcript, or an active file masking an anchored reset archive made that negative unsafe. The changed requirement keeps captured-transcript precedence and permits successor history only after a separate stable, strict, finalized-reset negative proof plus the new history's exact request anchor, persisted Smart reMarkable provenance, and strict response interval.
- The first plugin receipt reader used path-based =readFile= and could follow a symlinked =record.json= into a forged cached success. Both WhatsApp and cloud-upload receipt journals now use no-follow descriptor reads with owner/type/identity, private-mode, single-link, 64 KiB, stable-read, and fatal UTF-8 checks before parsing.
- The first transient runner waited only for the Smart Remarkable process and could leave a stale ready marker after its SSH tunnel died. An intermediate requirement made the runner supervise both children and made AppLoad independently health-check the fixed forwarded bridge before accepting either native-button trigger. The later asynchronous-capture requirement retains tunnel supervision but replaces that single readiness state with separate local-admission and remote-capability markers.
- The first trusted-origin implementation passed a plugin-realm object to OpenClaw's public =runContext.setRunContext= API before =chat.send=. OpenClaw 2026.7.1 validates plain-object prototype identity in the host realm, and the live write returned =false= before admission. A plugin-instance Map was considered next, but OpenClaw can keep separate startup, active-hook, and pinned-tool registries, so those Maps would not share authority. A realm-neutral scalar then passed split-registry unit tests, but live bind still returned =UNAVAILABLE=: the Gateway method was calling the ordinary plugin run-context facade after plugin registration had closed, so the late write could not be treated as a usable host mutation. The origin-v4/plugin-0.4 candidate retains that scalar authority and its exact run/transcript/context-version checks, but reaches host get/set/clear through a registered synchronous agent-event subscription. The emitter places only a random operation id on the plugin-owned stream; the operation, scalar, capability, and cleanup handle never enter the event or run id. Exact read-back, bounded in-flight commands, opaque-handle cleanup, and missing/asynchronous receipt failure preserve the original fail-closed authority model.
- Automatic survival across firmware updates is no longer required. The user
  accepts an exact, hassle-free reinstall after each update. This changes only
  update orchestration: exact runtime fingerprints, offline composition,
  independent rollback, root-read-only checks, and physical acceptance remain
  required.
- The first capture-confirmation handshake kept the stock marquee visible until native =chat.send= acceptance so remote failures remained retryable without another lasso. The user now requires Copy-like immediate feedback and non-blocking notebook interaction. The changed boundary is a validated, immutable in-memory crop: after that local boundary the stock selection closes and remote work continues in the still-single-flight worker.
- The first selected-crop path assumed every explicit selection was transparent handwriting over the stock gray tint and located it by searching the framebuffer for that tint. Stock Capture/image objects are opaque and cover the tint, while the handwriting whitening step destroys their light and color detail. Explicit buttons must now pass stock geometry plus both =selectionContainsStroke= and =selectionContainsImage= classification; image and mixed selections bypass handwriting normalization.
- The first trusted reMarkable prompt treated the visible selection itself as the complete current instruction. Capture turns can instead be referential to prior user instructions. The admitted server hook now gives canonical recent user context and durable memory precedence before applying the safe default of an in-depth explanation/background, without turning captured document text or ambiguity into tool authority.
- Immediate local dismissal originally assumed a later =write_back= would still target the same notebook view. Since an asynchronous user may navigate or edit while OpenClaw is working, the changed requirement makes insertion conditional on a fresh in-memory view-token match; WhatsApp remains the canonical result when that local target can no longer be proven.
- The first asynchronous write-back guard created its baseline only after close, checked that hash before a sequence of unverified Text-tool and placement taps, silently skipped unsupported characters, and then allowed an arbitrarily long response to type while physical navigation remained possible. A proposed exclusive-input repair was rejected before promotion because =EVIOCGRAB= can split a contact whose down event already reached =xochitl=. The supported guard now binds the post-close frame to the prepared original page, verifies the stock Text selection and closed palette, requires disjoint firmware-pinned toolbar and caret-like deltas immediately after placement, rejects any unsupported character or over-budget answer before the first key, and non-exclusively observes touch/pen contact before every emitted character. This does not manufacture a semantic stock page id; exact 3.28.0.164 caret and timing calibration is still required before promotion.
- The first strict descriptor consumer unlinked a stale trigger before parsing it but left the launcher's matching =busy= marker behind for the full transient lifetime. The listener now removes =busy= only for an exact byte-identical rejected generation, immediately accepts a later valid tap, and treats a mismatched or insecure generation as a fatal worker error so the runner's volatile-state cleanup owns recovery.
- The first stale-readiness fix made the launcher wait for a healthy SSH-forwarded bridge before it would hand a button descriptor to Rust. That kept remote startup and outage latency on the visible notebook path. Local capture readiness and capability-backed remote readiness are now separate: the stock selection can be validated, captured, and dismissed while the private tunnel comes up, but submission still fails closed until the remote-ready generation is current.
- The historical 3.28.0.164 QMD hash =2b9188af0c3fd726743e36ee1a3c86244cf6327ad22eeef1aa7a291a7add059d= predates the v2 descriptor and calls =--selection-button=write_back= or =--selection-button=whatsapp_only=. During app-first migration only, the launcher gives those exact old shapes a fresh random =legacy-v1= generation, Rust keeps their historical marquee detection and remote-accepted close behavior, and tests keep them strictly separate from v2. Canonical v2 replaces that boundary with stable QML orientation and nonce-bound prepare/capture/close acknowledgements before any remote work. The legacy route became inactive after v2 promotion but remains an approved rollback/migration state until deliberately removed.
- The first bridge accepted any long request id matching a generic safe-character pattern. Since OpenClaw run ids share a broader namespace, every Smart reMarkable boundary now requires the exact =smart-remarkable-= prefix and bounded suffix before reservation, authority binding, agent admission, or delivery.
- The first plugin capability probe ran only once before the bridge listener opened. A later Gateway reconnect could replace the loaded plugin while leaving process-lifetime readiness green. Capability proof is now tied to a monotonically increasing authenticated connection generation, invalidated on disconnect/reconnect, checked dynamically by health and every admission before journal reservation, and pinned across all RPCs for that job. The schema-v1/schema-v2 to schema-v3 migration is likewise explicit: old entries remain capacity-consuming 409 barriers rather than weakly replaying or becoming absent.
- The first origin gate relied on =before_prompt_build= alone and assumed the non-bundled plugin would receive sensitive hook access from its manifest or host defaults. Live OpenClaw policy now must explicitly grant both prompt injection and conversation access, plugin registration verifies those grants, and =before_agent_run= rechecks exact active authority plus the final trusted guidance immediately before model admission. This also records the pinned 2026.7.1 quirk that ordinary runs require an explicit pass result.
- Application and QMD artifacts were previously pinned and promoted by separate installers, which allowed protocol-skewed app-first or QMD-first states. The changed requirement makes =compatibility-3.28.0.164.env= one cross-artifact contract, distinguishes the approved legacy-functional QMD =2b9188af0c3fd726743e36ee1a3c86244cf6327ad22eeef1aa7a291a7add059d= from the historical 3.28.0.163 =PREVIOUS= reference =0fea5e9d78cb085528f0cde5af672abb9c3ca2b327127f43dc6e54605a688412=, retains both exact legacy launcher aliases only for the reviewed migration window, and requires QMD-before-app rollback. A later functional-hash refresh first used the isolated old contract to move exact deployed v2 QMD =28a253e1d16d4aa5e2852afa40699d3bc13b3fb2ab1e9cdc0a953deec9953ef6= to inert before installing the new contract, rather than weakening the new allowlist with another predecessor.
- The =0.8.0-openclaw= context feature adds an exact =DeviceSceneView= patch, so the new inert QMD cannot be installed under the old =0.7.3-openclaw= contract. The changed migration requirement therefore uses a special no-taps app-first boundary: install =0.8.0-openclaw= while the exact allowlisted =v2-migration-functional= QMD and old server remain; have the new client retain the strict historical one-image v2 bridge adapter while suppressing v2 local write-back; immediately move that exact v2 QMD to the new inert canary under the new contract; promote plugin =0.4.0=/origin-v4/journal-schema-v3 and the matching bridge together while inert; then promote only the exact v3 functional QMD. This exception does not permit taps during the app-first interval, silently reinterpret v2 as v3, weaken QMD-before-app rollback, or permit a functional v3 QMD against an older client/server.
- The first asynchronous listener supervision returned immediately when the trigger task died, detaching an in-flight processing task that might already have hidden the selection controls. The worker now cancels and boundedly settles or aborts processing, then emits the idempotent restore chord before exiting; a listener-owned RAII guard also removes local =ready= on every exit path.
- The first private prepare chord embedded its own =SYN_REPORT= even though =VirtualDevice::emit= appends another. The trailing automatic sync could report an error after the embedded sync had already activated QML Prepared, bypassing restoration. Handshake batches now contain only balanced key events, rely on exactly one library-owned sync, and treat every reported prepare failure as potentially side-effecting by attempting restore before return.
- Early write-back drafts allowed unbound legacy/pen-lasso insertion, blindly toggled the palette during uncertain cleanup, checked only new input events, and split modifiers across guard intervals. The supported candidate accepts only an exact v3 prepared-original document/page guard, inspects and verifies palette closure, queries current pen and all multitouch-slot state before output, and emits balanced body-style/per-character batches. V2 migration, legacy, and pen-lasso routes remain local-write-suppressed. Canonical WhatsApp delivery continues whenever these stricter local conditions suppress insertion.
- The first deployed v2 QMD used =AppLoadLauncher.launchApplication=. That singleton signal is broadcast to every loaded AppLoad QML receiver, and AppLoad's external-no-GUI handler starts the child but then assigns its PID to an undefined window, producing a live post-spawn QML error and allowing competing launchers with no reliable Rust trigger. The changed requirement commits visual pending state first, defers one exact-snapshot revalidation with =Qt.callLater=, directly calls one =AppLoadLibrary.launchExternal= operation, and treats only a positive PID as launch success. It does not patch AppLoad, add a boot service, or claim physical success from process spawn alone.
- The first direct-AppLoad revision generated its kernel nonce with GNU-style =/usr/bin/od -An -N32 -tx1=. Physical attempts at =08:38:13= and =08:54:38= proved that the valid descriptor reached the launcher, but the device's BusyBox =od= rejects =-A=; nonce creation therefore failed before busy/trigger publication and produced no Rust or server traffic. Because QML knew only that the child process started, its 45-second pending fallback explained the apparent long lag. The changed requirement uses the exact device-proven =hexdump= formatter, makes canonical nonce generation an installer capability gate, records content-free =SR_WAND= stages, and prewarms one parent-owned dynamic =AppLoadLibrary= per selection handler for reuse instead of recompiling it for all three phases. Those changes were guarded-deployed as application =0.7.2-openclaw= and physically proved the ink and Capture paths before the lifecycle failure below superseded that application revision.
- The first asynchronous capture revision treated the restricted tunnel as terminal: Rust waited only 50 seconds, and the runner published =bridge-failed= and killed Rust on the first SSH setup failure or later disconnect. It also admitted a button into any active locally-ready unit without reserving enough of the one-hour =RuntimeMaxSec=. Two physical requests later proved the image and ink path, but a subsequent fast local close created no server reservation after the session/tunnel had ended. The first recovery draft then incorrectly assumed OpenSSH =-G=, =ConnectTimeout=, =ConnectionAttempts=, and =ServerAliveCountMax= semantics on the exact Dropbear client; those options were removed before deployment in favor of non-networking Dropbear capability proof, parent-owned setup health bounds, and native =-K 30=. The changed requirement keeps the private crop only in RAM while the runner reconnects, gives each explicit request a serialized fresh lifetime, tolerates the =RuntimeMaxSec= stop race only after a fail-closed state recheck, waits until the stopped fixed-name transient is actually unloaded, and retries an interrupted or temporarily unavailable OpenClaw transport with the same idempotency identity while refusing redirects. A durable tablet outbox remains prohibited by the no-retained-crop requirement. This revision is now guarded-deployed as =0.7.3-openclaw=, and two physical ink taps prove its ordinary one-tap/one-turn path; deliberate tunnel-loss recovery remains a separate acceptance case.
- The first live server proof used OpenClaw's runtime plugin-inspection CLI. That command creates a second plugin registry and left resource-intensive =openclaw-hooks= workers rather than observing only the production Gateway. The supported deployment must never run that inspector. It proves the active plugin through a hard-timeboxed direct =GatewayClient= query to the already-running loopback Gateway, current-PID journal evidence, exact live manifest/source hashes, static hook/tool registration checks, and absence of registration failure; the evidence must state that live hook-count enumeration is unavailable instead of launching another runtime.
- The first guarded origin-v4 promotion required the persistent watchdog timer to report =SubState=waiting= immediately after start. An overdue =Persistent=true= timer may correctly fire its one-shot before reaching that state, so the check rolled back a healthy candidate. The revised requirement first proves the timer enabled and active, then requires both a fresh =LastTriggerUSecMonotonic= and fresh service =InvocationID=, clean one-shot completion, and only afterward the final active/waiting timer state. Missing triggers, failed one-shots, or a timer that does not return to waiting still fail closed.

* Immediate next steps

1. [X] Verify the trigger-routing and trigger-file regression tests.
2. [X] Cross-build the aarch64 binary with the pinned Rust toolchain.
3. [X] Deploy the binary and one-shot launcher under =/home/root/smart_remarkable=.
4. [X] Lasso a disposable handwritten question and verify one answer is drawn below it.
5. [X] Confirm the Smart Remarkable process exits, =xochitl= stays active, and the root filesystem stays read-only.
6. [X] Deploy and validate the print-specific selection prompt through the repository's runtime prompt override.
7. [X] Verify that the same native-selection workflow creates a stock text box below the selection and no pen strokes.
8. [X] Enable and smoke-test OpenClaw's authenticated Chat Completions endpoint on the private gateway.
9. [X] Add a restricted, forwarding-only tablet tunnel and route the one-shot launcher to =openclaw/default=.
10. [X] Remove the direct OpenAI credential from the active tablet configuration and verify one live lasso-to-OpenClaw-to-stock-text round trip.
11. [X] Patch the OpenClaw transport to send the exact canonical session/channel headers, omit =user= in that mode, and default to =openclaw/main=.
12. [X] Run the native transport and Paper Pro regression tests, then cross-build a fresh aarch64 binary.
13. [X] Package a non-QTFB AppLoad arming tile with a transient, ten-minute, =xochitl=-bound worker and no injected extension.
14. [X] Transactionally install the package under =/home= on the new Paper Pro 3.28.0.163.
15. [X] Verify exact-session response routing, one-request arming/cancellation, T.M.R. interlock configuration, stock UI health, cleanup, and read-only root.
16. [X] Reproduce the post-restart Paper Pro capture failure and identify the valid framebuffer among multiple =card0= allocation groups without changing notebook content.
17. [X] Add and test a bounded multi-candidate framebuffer locator.
18. [X] Transactionally deploy the corrected binary and verify a read-only live capture on the current =xochitl= allocation layout.
19. [X] Move duplicate rollback bundles out of AppLoad's application tree without deleting them and prevent future deployments from recreating duplicate tiles.
20. [X] Replace the selection-clearing four-finger workflow with automatic submission on completion of the armed pen lasso.
21. [X] Run one native lasso-to-stock-text round trip on a deliberately disposable page and confirm the arm-then-lasso AppLoad workflow.
22. [X] Implement and locally test =once=, =session-hold=, and =session-auto= with a separate root-only settings file.
23. [X] Add and locally test single-request admission, held-lasso recognition, stale-selection suppression, and per-cycle memory cleanup.
24. [X] Replace the raw native extension candidate with inert and functional firmware-3.28.0.163 QMLDiff artifacts, record the exact compatibility allowlist, and verify both offline against the extracted target resource tree.
25. [X] Build a transactional button installer with a bounded =xochitl= health watchdog and preserved rollback outside active Xovi extension paths.
26. [X] Wake and freshly fingerprint the exact tablet; run the live read-only compatibility probe against its current hashtable and stop without mutation if any value differs.
27. [X] Transactionally stage the disabled =LLM= visual canary under =/home=, activate it only under the watchdog, and verify the stock UI remains healthy before considering the functional patch.
28. [ ] Promote to the functional button only after the inert canary passes; validate two button-triggered selections in one session plus the hold and automatic fallbacks on a disposable page, then confirm cleanup and stock-UI invariants.
29. [X] Refresh the installed button through a new after-Copy inert canary and exact-confirmation functional promotion; retain the old functional QMD as a pinned precondition and preserve watchdog rollback to stock.
30. [ ] Verify in no-submit mode that one button tap highlights immediately, closes the marquee only after a successful crop, leaves the underlying strokes untouched, and makes no OpenClaw request; then repeat one canonical live request and recheck stock-UI invariants.
31. [X] Extend and independently review the exact-device QML allowlist for the installed Pen Layer Memory QMD and optional exact Quick Settings Timer QMD, reactivate Xovi through a stock-rollback watchdog, and require the complete installed set during functional promotion.
32. [X] Implement and test a loopback-only, narrow-token server bridge around native Gateway =chat.send= plus a receipt-checked no-transcript-mirror WhatsApp plugin method, with image attachments, exact canonical-session routing, idempotent "reading and working" acknowledgement, unchanged canonical session settings, and final-result retrieval.
33. [X] Add =write_back= and =whatsapp_only= trigger sources to the single Smart Remarkable worker; prove that only =write_back= can invoke =draw_text= and that both clear request image/tool state.
34. [X] Build firmware-3.28.0.163 inert and functional QMLDiff artifacts that render =Cut → Copy → LLM → Send → Convert to text → Delete= and carry distinct explicit actions without static AppLoad imports.
35. [X] Extend and independently review the exact-device coexistence allowlist for every currently installed ReMagic QMD before allowing either two-button canary to reach =ARMED=.
36. [X] Visually confirm guarded disabled transaction =20260725T163235Z-44111= shows =Copy → LLM → Send → Convert to text= correctly, then promote only that exact transaction through the bounded stock-rollback watchdog. Functional transaction =20260725T211315Z-12106= committed the reviewed QMD.
37. [ ] On a disposable selection, verify the notebook-with-sparkles action produces one WhatsApp acknowledgement/activity/final sequence and one matching stock-text insertion; verify the sparkles action produces one WhatsApp sequence and no tablet insertion; then recheck delivery idempotency, cleanup, =xochitl= restarts, inactive services, and read-only root.
38. [X] Replace Git-diff-only installer provenance with a deterministic staged-file SHA-256 manifest, verify the transferred device installer and exact extracted file set, preserve the manifest trust chain in root-only recovery metadata, and cover untracked staged inputs in a device-free regression.
39. [X] Add and test transient selected-handwriting normalization and bounded high-resolution upscaling before request fingerprinting and submission.
40. [X] Add and test the strict server-owned =received_text=/=response_text= response envelope, atomic =I read:= WhatsApp final, answer-only write-back, and explicit =[unclear]= behavior.
41. [X] Make accepted canonical turns resilient to delayed or missed Gateway final events by recovering only an attributable response from bounded canonical history, and extend the bounded client/server request window for ordinary OpenClaw tool work.
42. [X] Deploy only the tested bridge and reversible =/home=-scoped worker update; do not change the installed QMLDiff, firmware, boot configuration, or system partition.
43. [ ] On disposable handwriting, verify the WhatsApp quote against the actual selection in both icon modes, verify answer-only notebook insertion for notebook-with-sparkles, and recheck provider receipts, idempotency, cleanup, =xochitl= restarts, and read-only root.
44. [X] Replace the two implementation labels with exact stock sparkling-notebook and sparkles resources; centralize pending-state reset; and implement the then-current supervised tunnel/worker plus bridge-health-gated readiness design. The later asynchronous-capture requirement supersedes that last readiness boundary with steps 53 and 56.
45. [X] Add trusted, bounded reMarkable origin admission, durable input provenance, exact captured-transcript recovery after canonical-session remapping, and a gated receipt-checked PDF/EPUB reMarkable-cloud delivery tool; replace unusable late public-facade writes with the synchronous agent-event host run-context adapter and pin the reviewed plugin metadata at version =0.2.2=.
46. [X] Transactionally deploy reviewed plugin version =0.2.2= and the bridge, plus the reversible =/home=-scoped worker/button refresh; verify server and tablet rollback preconditions before mutation.
47. [ ] Run one disposable request through each icon and one harmless document-delivery smoke request, then verify WhatsApp receipts, answer-only insertion, cloud artifact receipt, inactive helpers, unchanged =xochitl= restart count, and read-only root.
48. [X] Capture the Paper Pro 3.28.0.164 stock executable, build ID, rebuilt hashtable, exact resource tree, surviving Xovi/AppLoad hashes, and a Mac-side =/home= preimage while stock =xochitl= remains stable.
49. [X] Build the 3.28.0.164 inert and functional stock-icon QMDs, update the exact co-resident allowlist, and compose all eight QMDs offline into 22 resources with no compatibility errors.
50. [X] Install the signed 3.28.0.164 packages and refresh the button through the independent rollback canary; verify AppLoad mappings, zero restarts, inactive takeover helpers, and read-only root.
51. [X] Record the exact package hashes, Gestik-settings before/after hash, activation transaction, postchecks, and rollback procedure in the shared update recipe.
52. [ ] On a disposable page, physically submit one handwritten selection through each 3.28.0.164 stock icon; verify answer-here inserts only stock text, sparkles inserts no tablet text, WhatsApp receives both canonical responses, and the final process/root invariants remain unchanged.
53. [X] Add and locally test the versioned stock-selection descriptor, exact image/mixed rectangle crop, strict volatile trigger parsing, and nonce-bound local prepare/capture/dismissal boundary.
54. [ ] Preserve capture RGB/tonal detail while retaining the established ink normalization, bind selection kind into request fingerprints and origin admission, and add real opaque-selection fixtures. The implementation and deterministic synthetic RGB/mixed regressions are complete; one real device-captured opaque fixture remains required.
55. [X] Add capture-aware canonical-history/memory intent precedence and safe in-depth-background fallback to the admitted OpenClaw hook while preserving the strict two-field response envelope and reMarkable-only tool gate.
56. [X] Remove avoidable button-path latency without weakening tunnel health, single-request admission, response attribution, or write-back target identity; record deterministic device-free structural coverage that local-ready admission and prepare/capture/close precede tunnel health and the bridge-ready wait; and prove that a changed view suppresses tablet insertion. Physical UI timing remains part of step 59.
57. [X] Run the complete Rust, shell, bridge, plugin, QML compatibility, and exact-resource composition suites; cross-build and independently review the new aarch64 worker and QMD bytes.
58. [X] Deploy only through a fresh exact-3.28.0.164 preflight, inert canary, human visual confirmation, functional canary, watchdog, and rollback; keep stock =xochitl= stable and =/= read-only. The direct-launch refresh used inert transaction =20260731T222234Z-48219= and functional transaction =20260731T222432Z-49869=.
59. [ ] Physically accept normal and rotated-180-degree ink, Capture/image, and mixed selections through both icons, including exact geometry, prepare/capture/close chrome restoration, immediate marquee close, continued notebook responsiveness, WhatsApp =I read:= accuracy, default background behavior, guarded Text/palette/caret placement, no-output agent mode, pre-existing and mid-output pen/touch interruption, the 600-key/time budget, two-request busy/rearm behavior, tunnel/listener failure cleanup, and final stock-UI invariants.
60. [X] Add and device-free test the shared offline app/QMD compatibility contract, app-side Smart-QMD classifier, exact-client functional-promotion gate, common deployment lock, staged/recovery provenance, legacy 3.28.0.164 migration state, and QMD-before-app rollback ordering; after independent review, finalize all six generation hashes to the exact rebuilt artifacts.
61. [ ] On a disposable selection, physically verify that one tap paints selected feedback, creates exactly one AppLoad external launch, produces no =appload.qml= undefined-window error, publishes one busy/trigger generation consumed by Rust, closes the marquee promptly, and leaves the notebook responsive; then complete the normal/rotated ink, Capture/image, mixed, answer-here, agent-only, interruption, cleanup, and stock-process acceptance in step 59.
62. [X] Complete the local compatibility, composition, shell, and independent-review gates for source/QMD/protocol/launcher hashes =a4af1eeff5f011479e68e8fc14fe385e6a5070e8ff3051e0df69ff18c93a5b03=, =495db83da318801d24ae3d4d63120c7e9d1568145e3298efccecea583f5c17c4=, =4317dafd6fcc3a1cd5fd21b427be7465621f570443112a390840004a0d23203a=, and =df7177e7d75b15a521e5ed748c8ea01f867deb3953f0cf3ac1dc5dd58f8aa88b=; then deploy application =0.7.2-openclaw= through a fresh exact-firmware preflight including the live =hexdump= nonce canary, old-contract refresh-inert transaction =20260801T091905Z-53220=, staged manifest =360f2a54f5efb1272dd21e2dcc421b5a8e329b9ffabdbd7ff95b3185e3360188=, and refresh-functional transaction =20260801T092129Z-55163=. Final guarded evidence was =xochitl= PID 48260, =NRestarts=0=, and read-only root. This is deployment completion only.
63. [X] On disposable selections, complete two post-=0.7.3= physical wand taps. Each produced one content-free =SR_WAND= chain from click through =trigger-published=, one Rust consumption, one =whatsapp_only= server reservation, and one completed OpenClaw turn. Request =smart-remarkable-fa26-18c7b415578ef44d-0= completed at =14:16:39Z= and request =smart-remarkable-102b9-18c7b68a6b9a9b5c-0= completed at =15:01:39Z=; the user confirmed the interaction worked. Post-request state retained =xochitl= PID 57254, =NRestarts=0=, read-only root, ready tunnel, and no busy/trigger/ack residue. Resume the wider physical matrix in steps 61 and 59 without treating this ink/wand acceptance as Capture, write-back, rotation, interruption, or outage coverage.
64. [X] Trace the reported fast-close/no-send boundary without inspecting selected content: prove two earlier schema-v2 physical requests completed with native WhatsApp acknowledgement/final =sent= receipts, prove the later tap created no server reservation while the restricted tunnel was absent, leave the healthy bridge/Gateway unchanged, and implement =0.7.3-openclaw= with serialized fresh explicit-button lifetime, bounded in-memory tunnel reconnect, and same-request-id HTTP/body recovery. Cross-build the exact aarch64 worker and pass the targeted Rust plus all six shell suites.
65. [X] On the serial-pinned tablet, pass the exact firmware/build/stock-hash, read-only-root, Dropbear-2025.88, native =-K=, required-option, =flock=, and idle-runtime preflight; move the =0.7.2= functional QMD to inert under its exact contract with transaction =20260801T115255Z-35025=; install =0.7.3-openclaw= as application transaction =20260801T115433Z= with staged manifest =afa3ee5e5e7edd24c3c059fc1015ba42294083e185e661d7ed2775d0908773aa=; and promote the unchanged functional QMD with current-contract transaction =20260801T115608Z-37109=. A request-free start/health/stop smoke test proved local and bridge readiness without selection markers or a model request. Final evidence is =xochitl= PID 57254, =NRestarts=0=, inactive Smart/Riddle/deployment units, and read-only root.
66. [ ] Physically tap one disposable wand selection with the restricted tunnel initially unavailable, restore Wi-Fi inside the fifteen-minute bridge-readiness window, and verify prompt local close, exactly one schema-v2 reservation/OpenClaw turn, matching same-id replay if transport was interrupted, both native WhatsApp receipts, no retained crop file, stable =xochitl=, and read-only root. Then finish the wider step-59 matrix.
67. [X] Implement the =0.8.0-openclaw= request-context generation: exact-firmware v3 document/page binding, bounded no-follow display-name resolution, one same-frame prepared selection plus faithful current-page-view capture, strict =selection-page-v1= two-image transport, fixed server attachment roles, selection-only =received_text= attribution, and proactive best-effort OpenClaw guidance with the existing side-effect authority boundary.
68. [X] Finish the complete Rust, shell, bridge, plugin, privacy, journal, retry, exact-resource QMLDiff compatibility/application, co-resident composition, and aarch64 cross-build gates; close independent review of the resulting worker, launcher, protocol, QMD, bridge, and plugin artifacts before any live mutation. The final gate passed 107 applicable Rust all-target tests, 148 Node bridge/plugin tests, and all six shell suites. Both compiled QMDs passed compatibility, actual application, and QML parsing alone and in the exact seven-hack co-resident 3.28.0.164 composition. The host canary controller now repeats compiled-QMD =apply-diffs= against both exact extracted stock resources with the SHA-pinned live hashtable and parses both outputs with local =qmlformat --ignore-settings= before its first remote write. Final worker SHA-256 is =4c9605f7f9e6be898230c3c5d607fa36fc1ce815ad85cc8f6f04e625be314f1e= with build ID =16bc36a982fbb2465375641a2006e3936b511394=, maximum GLIBC 2.28, and no RPATH/RUNPATH. This is local release evidence, not live deployment or physical acceptance.
69. [X] Deploy the context generation only through the special paired, no-taps app-first migration. Application transaction =20260801T185142Z= installed exact =0.8.0-openclaw= with staged manifest =54f2c2cb0b9c9d828ed7aaa429ac7f351cd4bb384d0d496ba46ae83417e7233a= while the allowlisted v2 functional QMD and plugin =0.3.0=/old bridge remained. Revised r3-aware refresh-inert transaction =20260801T185306Z-95906= committed exact disabled QMD =635752321485a4dfb702b24fdf9b1f836f329a1399ebcc06f4b19dc4035a625a= and received visual confirmation. Guarded server transaction =20260801T213248Z-32250= then promoted plugin =0.4.0=/origin-v4/response-envelope-v3/journal-schema-v3 and the matching bridge with current-process capability proof and a clean fresh watchdog invocation. Refresh-functional transaction =20260801T213623Z-74175= finally committed exact v3 QMD =5cf5156df227a1ecdf3fb421b2cc57e55564bf31ab9d434a7f16c21b40ef0dc2= on =xochitl= PID =88214= with =NRestarts=0=, inactive canary/takeover units, every co-resident QMD unchanged, and =/= read-only. This completes deployment, not step-70 physical request acceptance.
70. [ ] On a disposable fit-to-page selection, physically verify that one wand tap reaches canonical OpenClaw with exactly the same-frame selected-region/current-page attachments plus the correct document-id-bound display name/page metadata; verify WhatsApp quotes only the selection, uses page/history/memory for a useful proactive answer, and performs no unauthorized side effect. Confirm no raw PNG or raw document-name request field is retained in tablet scratch state or the bridge request journal; the title intentionally remains eligible for canonical OpenClaw history and may appear in a useful cached answer. Repeat once zoomed/panned to verify honest =viewport_only= labeling, then recheck =xochitl=, read-only root, cleanup, and both response modes.
71. [X] Reproduce and locally fix the post-admission automatic canonical-session-successor race: prefer a captured transcript that owns the exact request even while its assistant is pending, refuse unscoped live-final crossover, admit newly canonical bounded history only after exactly one finalized captured reset archive is strictly and stably proven unanchored, require exact persisted Smart reMarkable provenance, reject unverifiable successor evidence, and pass the focused plus complete bridge test suites.
72. [ ] Guardedly promote the bridge-only rollover fix, then physically verify one disposable request completes without a reminder even if OpenClaw rotates =agent:main:main= during admission; recheck exact WhatsApp final delivery, answer-here behavior, bridge journal completion, and unchanged tablet safety invariants.

* Verification notes

** Current two-button implementation and deployment history

- Read-only server tracing found exactly two post-=0.7.2= schema-v2 requests: =ink= reserved at =09:43:39.975Z= and completed at =09:44:04.643Z=, and =image= reserved at =09:45:05.219Z= and completed at =09:45:21.293Z=. Both reached native Gateway =chat.send= and committed acknowledgement and final WhatsApp receipts with status =sent=. The later reported tap created no reservation, admission, Gateway call, receipt, completion, or server error; at that time there was no =smart-remarkable-tunnel= SSH process, while bridge PID 634000 and Gateway PID 633674 were active with =NRestarts=0= and bridge health 200. This proves a pre-server tablet tunnel/lifetime failure; it does not substitute for the unavailable tablet-side =xochitl=/root postcheck.
- Deployed =0.7.3-openclaw= hashes are worker =d9e045c8eee9a442def7bf6218cfbf81797a3133cdd49673746931a1b0e222bb=, launcher =e8d8729997f4975de54a9a3e6c20d219c4aeb069bc9268a758b3b81bd53582f3=, reconnecting runner =72588acb490cb17b7a2b8ca3bce4dc938862cff95da922267074d5c14f5b232c=, unchanged protocol =4317dafd6fcc3a1cd5fd21b427be7465621f570443112a390840004a0d23203a=, and contract =fab7ddfb35b0b005f382d2a2e8450b44b1e1a38adb1189546b3e7da860cf331d=. The binary build ID is =3acb24c18156394f0352ad24572bdfe813e07507=, its interpreter is =/lib/ld-linux-aarch64.so.1=, maximum GLIBC is 2.28, and it has no RPATH/RUNPATH. Seventeen OpenClaw transport tests pass, including exact serialized-request retry before headers, after accepted-header/body loss, and across a temporary 503; redirect, status-classification, sensitive-header, and cancellation coverage also pass. Twenty touch-targeted tests and all six shell suites pass. The expanded 88-test library run passed 87 product-relevant tests and hit only the pre-existing hard-coded =render_chinese_font_test_png= output-path failure; no product path failed. Guarded deployment in step 65 used staged manifest =afa3ee5e5e7edd24c3c059fc1015ba42294083e185e661d7ed2775d0908773aa= and preserved the exact functional QMD.
- Post-deployment physical acceptance added exactly two schema-v2 records to the production journal, increasing it from 15 to 17. Both were =whatsapp_only=, =ink=, and =completed=: =smart-remarkable-fa26-18c7b415578ef44d-0= ran from =14:16:23Z= to =14:16:39Z= and =smart-remarkable-102b9-18c7b68a6b9a9b5c-0= ran from =15:01:23Z= to =15:01:39Z=. The bridge remained PID 634000 and Gateway PID 633674 with zero restarts; tablet =xochitl= remained PID 57254 with zero restarts and read-only root. No response or selected content was inspected to establish this evidence.

- The v3 device-free Rust coverage adds canonical document/page descriptor and acknowledgement binding, exact v2-migration isolation, same-frame selection/page capture, bounded no-follow stable =visibleName= resolution, selection-page fingerprinting, context/debug-persistence refusal, selection-only response attribution, context cleanup, and v2 local-write suppression to the earlier nonce/kind/orientation/geometry/time, prepare/close, framebuffer, write-back, input-monitor, lifecycle, and transport-retry coverage. Product-relevant local tests pass with only the unrelated upstream hard-coded font-output-path test filtered; the final consolidated cross-build/artifact review completed in step 68 and remains local rather than live evidence. The complete local cross-language, composition, cross-build, and independent-review gate in step 57 and guarded promotion in step 58 apply to the prior direct-launch generation. The narrower BusyBox/cached-helper gate and guarded deployment are complete in step 62, and ordinary post-fix physical request acceptance is complete in step 63; the wider physical matrix remains steps 59 and 61.
- The local loopback bridge and no-transcript-mirror OpenClaw plugin suite contains 165 Node tests. In addition to the previous routing, delivery, journal, and sandbox cases, it covers exact =selection-page-v1= header/body matching, ordered =selection=/=current_page= roles, per-image/aggregate/body limits, NFC/control-clean bounded title and page metadata, fingerprints over both images and canonical metadata, two fixed Gateway attachment names, selection-only =received_text=, response-envelope v3, origin-v4/plugin-0.4 capability binding, proactive best-effort employee guidance with explicit side-effect limits, schema-v1/schema-v2 fail-closed migration barriers under schema v3, strict stable finalized-reset negative proof including duplicate-session-header rejection, an immediate pre-read file-size bound, pre-blank line-size enforcement, and narrow JSON-whitespace handling, exact persisted successor-session provenance, pending captured-session precedence, live-final crossover refusal, and the prior history, duplicate/truncation/cross-user attribution, receipt, run-context, reconnect, capacity, tool-authority, and no-follow cases. Those tests remain local behavior evidence; the separate guarded transaction and live probes below establish deployment.
- The currently installed server plugin is version =0.4.0= with its matching origin-v4/schema-v3 bridge from guarded transaction =20260801T213248Z-32250=. A fresh direct production-Gateway probe reported exact =selection-page-v1= capabilities and ordered attachment roles on Gateway PID =763455=; bridge PID =764888= returned healthy; both had zero restarts; configuration hash, request-journal inode, and the 18 pre-existing records remained unchanged; the watchdog completed a fresh invocation and returned active/waiting; and no inspector worker remained. Bind and clear require =operator.admin=, while =smart_remarkable.deliver= remains the narrow =operator.write= WhatsApp method. Historical server transaction =20260728T094046Z-7054958= promoted version =0.2.2= with its tested bridge and retained a complete rollback bundle; that record is preserved as history, not current state.
- The deployed =0.8.0-openclaw= artifact contract pins worker =4c9605f7f9e6be898230c3c5d607fa36fc1ce815ad85cc8f6f04e625be314f1e=, launcher =6660d1f01510d9e92910f9fbdcbd23a4fe4c40aac3d5a74a27213fe3faea14cd=, reconnecting runner =72588acb490cb17b7a2b8ca3bce4dc938862cff95da922267074d5c14f5b232c=, and v3 protocol helper =a11af20d55fc668c59e367d49e834844ed1e9a041cd400de481690811afb750c=. Functional source/compiled QMD hashes are =1b01d2a123ac5d16763b140c2342c14bbd99243455fc81debdad85165707644d= and =5cf5156df227a1ecdf3fb421b2cc57e55564bf31ab9d434a7f16c21b40ef0dc2=; inert source/compiled hashes are =85577aabce320c983de04ef0928851a9567839ba95494bd2d58dbf14f50b7b25= and =635752321485a4dfb702b24fdf9b1f836f329a1399ebcc06f4b19dc4035a625a=. The final binary build ID is =16bc36a982fbb2465375641a2006e3936b511394=, its interpreter is =/lib/ld-linux-aarch64.so.1=, maximum GLIBC is 2.28, and it has no RPATH/RUNPATH. Device readback after functional transaction =20260801T213623Z-74175= matched the worker, staged manifest =54f2c2cb0b9c9d828ed7aaa429ac7f351cd4bb384d0d496ba46ae83417e7233a=, contract =9a847c23e2a25d4554d10072c7f3ba706c01ae7743922463767c2e968b374215=, and functional QMD. These are deployment identities, not physical-request acceptance evidence.
- The first live =0.8.0= refresh-inert attempt, transaction =20260801T184459Z-74499=, failed closed at =prearm= because the tablet carried reviewed BetterTOC r3 SHA-256 =903e40e97d6d48923c6f76f77b5ff7f20ac156ee2b01ee266f51e4487dfe68fb= while the candidate contract still pinned r2. It did not arm, replace the old functional QMD, or restart =xochitl=: the old QMD remained =495db83da318801d24ae3d4d63120c7e9d1568145e3298efccecea583f5c17c4=, =xochitl= remained PID =77164= with =NRestarts=0=, and =/= remained read-only. The revised contract preserves and pins r3. Application transaction =20260801T185142Z= then installed exact worker =4c9605f7f9e6be898230c3c5d607fa36fc1ce815ad85cc8f6f04e625be314f1e= with revised contract =9a847c23e2a25d4554d10072c7f3ba706c01ae7743922463767c2e968b374215= and staged manifest =54f2c2cb0b9c9d828ed7aaa429ac7f351cd4bb384d0d496ba46ae83417e7233a=. Guarded refresh-inert transaction =20260801T185306Z-95906= committed and validated exact disabled QMD =635752321485a4dfb702b24fdf9b1f836f329a1399ebcc06f4b19dc4035a625a= on =xochitl= PID =84925= with =NRestarts=0=, no deployment lock or active assistant/canary unit, and read-only root. Visual confirmation, paired server promotion, and functional QMD promotion later completed in step 69; physical use remains step 70.
- The deployed v2 aarch64 worker SHA-256 was =c73586e65fe6acc5333b95c5934a9f5298ec5126de1069504ed09182c05e08a5= with build ID =63c2a311d60699e22a22ee54e90094cce2e587f8=, interpreter =/lib/ld-linux-aarch64.so.1=, maximum GLIBC 2.28, and no RPATH/RUNPATH. Guarded transaction =20260731T162912Z-38343= first promoted source/compiled QMD hashes =130353dbba7fd31b764f0835b610d59d9c25c7f1cc2b2c52e9285379f23ec1b8= and =28a253e1d16d4aa5e2852afa40699d3bc13b3fb2ab1e9cdc0a953deec9953ef6=; live taps then exposed the AppLoad broadcast/no-GUI failure. The next direct-launch source/compiled hashes were =6aa2e491cffa568458c696e9035dca31f02b66786e30ad6f12f67dbfaa5b1fb9= and =3ad5c084765a980b017da4b5e87670312242212ea362a456b7ab487d2ca9b451=, with protocol =e124286e273474782f1632402711ae30ab6643afe8f05e85aecc9ce43cfc1e74= and launcher =79845482e8a47c84ee73b02f1641e60f83c81cd44504914f1aa49a1d797107e1=. Old-contract refresh-inert transaction =20260731T222234Z-48219= safely committed inert before application staged manifest =5690a3e627c5fa82f02dba631616522ebdf0278c6d91eb2bbccf0db339b20a54= installed the unchanged worker; refresh-functional transaction =20260731T222432Z-49869= then committed that QMD with final =xochitl= PID 39042 and =NRestarts=0=. Physical attempts at =08:38:13= and =08:54:38= delivered a valid descriptor to the launcher, where BusyBox =od= rejected =-A=; no nonce, busy/trigger marker, Rust event, or server request followed, and the 45-second pending timer accounted for the apparent lag. The later =0.7.2-openclaw= source/QMD/protocol/launcher hashes were =a4af1eeff5f011479e68e8fc14fe385e6a5070e8ff3051e0df69ff18c93a5b03=, =495db83da318801d24ae3d4d63120c7e9d1568145e3298efccecea583f5c17c4=, =4317dafd6fcc3a1cd5fd21b427be7465621f570443112a390840004a0d23203a=, and =df7177e7d75b15a521e5ed748c8ea01f867deb3953f0cf3ac1dc5dd58f8aa88b=. Old-contract refresh-inert transaction =20260801T091905Z-53220= preceded staged application manifest =360f2a54f5efb1272dd21e2dcc421b5a8e329b9ffabdbd7ff95b3185e3360188= and refresh-functional transaction =20260801T092129Z-55163=. Final guarded evidence recorded =xochitl= PID 48260, =NRestarts=0=, and read-only root. Two subsequent physical requests proved ink and image/Capture delivery before the session-lifetime failure that =0.7.3-openclaw= supersedes.
- The prior 3.28.0.163 stock-icon functional two-button QMD SHA-256 was =0fea5e9d78cb085528f0cde5af672abb9c3ca2b327127f43dc6e54605a688412= and its matching disabled canary SHA-256 was =0e5eec4ffa03b2b0fdbc6b42519165f93ae77a00c01cf77d92db8993d934978e=. Both applied offline to that exact extracted resource tree as =Cut → Copy → notebook-with-sparkles → sparkles → Convert to text → Delete=. The user visually accepted guarded disabled transaction =20260728T095031Z-58185=, and guarded transaction =20260728T104334Z-63134= promoted that exact historical layout before the 3.28.0.164 update.
- Server source transaction =20260725T230143Z= atomically promoted all eleven tested bridge modules and retained the previous source as a same-filesystem rollback. The bridge returned healthy on PID =389425= with zero restarts. The OpenClaw Gateway remained on PID =372756= with zero restarts; the system unit hash, WhatsApp plugin hashes, and request-journal inode remained unchanged.
- Independent deployment review passed the fail-closed installer, canonical helper-path validation, empty-environment health probe, exact-device/QMD allowlist, watchdog rollback, root-read-only boundary, and dedicated-account restricted-tunnel design.
- The staged-file provenance regression passes locally. It proves stable path ordering and checksums, changes the manifest when an untracked-named staged input changes, rejects staged symlinks, and syntax-checks both installer halves. No tablet or server was contacted.
- The recovery-metadata regression executes the real installer helper and proves that a synced =phase=prepared= record exists before either application rename, survives simulated power loss with the old app moved or the new app active, and is atomically replaced by =phase=installed= with the embedded manifests after verification.
- The artifact-compatibility regression accepts exactly two repository states: all final-generation fields explicitly =UNRESOLVED=, which blocks application installation and functional QMD promotion before network or device access; or a complete contract whose six hashes equal the actual local worker, launcher, runner, protocol helper, source QMD, and compiled QMD. The current complete BusyBox/cached-helper contract passed independent review and guarded deployment in step 62. The regression also covers exact QMD-state classification, malformed/duplicate/unknown contract rejection, bundle membership and modes, the shared deployment lock, exact-client recheck before =ARMED=, recovery provenance, both pinned legacy button spellings, and unambiguous =LEGACY= versus historical =PREVIOUS= semantics. Its host-side result is not physical request evidence.
- The two-button protocol regression provides deterministic device-free local-handoff timing structure: AppLoad admits from local =ready= with no bridge marker or health probe; the runner starts Rust before SSH and SSH before its bridge health loop; and Rust completes prepare acknowledgement, explicit-orientation capture, and close acknowledgement before the bridge-ready wait. It accepts canonical one-digit coordinates, rejects leading-zero, non-digit, and overlong coordinates before BusyBox integer conversion, verifies the exact =hexdump= nonce command and lowercase-hex result, requires the installer capability canary and content-free =SR_WAND= stages, and verifies two distinct button destinations. Rust regressions separately prove listener-owned readiness cleanup and priority supervision. Host-side structure and guarded installation do not replace post-fix physical latency/request acceptance in steps 63 and 59.
- The dedicated password-locked server account =smart-remarkable-tunnel= passed live positive forwarding to only =127.0.0.1:18792= and live denials for Gateway port =18789=, the =localhost= alias, reverse TCP, local and remote StreamLocal forwarding, shell/exec, and PTY. The temporary test key and sockets were removed, the permanent authorized-key file returned byte-for-byte to its pretest hash, and the normal =mdf= keys were never changed.
- Tablet worker transaction =20260725T230352Z= installed binary SHA-256 =0bce9522c47aa2becc2f07171ed59ade012061ff33bd5cdbc11ec1c94eefde50= with staged-manifest SHA-256 =7a68ae2303e176b96014c34f559f641c6130646dcb8f1e7e8b616479293b9ab1=, an exact verified manifest, =phase=installed= recovery record, and preserved previous bundle. A start/health/stop tunnel smoke test created no model request and left the request-journal entry count unchanged. =xochitl= stayed on PID =60746= with zero restarts, the installed functional QMD stayed byte-identical, =/= stayed read-only, and all Smart/Riddle/installer units and processes returned inactive.
- Live harmless request =sr-smoke-20260725T230720Z= exercised the promoted =whatsapp_only= bridge. OpenClaw's canonical final was exactly ={"received_text":"Bridge smoke test\nPlease reply READY","response_text":"READY"}=; both acknowledgement and final returned native WhatsApp =sent= receipts, while the tablet-facing body exposed only the fixed WhatsApp-only receipt.
- Live request =smart-remarkable-origin-canary-20260728T094530Z= exercised the deployed =0.2.2= origin adapter in the canonical =agent:main:main= session. Its durable turn recorded =sourceChannel: remarkable= and reMarkable external-user provenance, OpenClaw returned the exact requested marker, and acknowledgement plus final both received WhatsApp =sent= receipts.
- The refreshed =/home=-scoped worker has the reviewed SHA-256 =0bce9522c47aa2becc2f07171ed59ade012061ff33bd5cdbc11ec1c94eefde50= and staged-manifest SHA-256 =845e1fa437fb86b8be2b60efe7a95b0600f46c5b314223e1b68e8d569b3df502=. After functional promotion, a request-free launcher smoke test started the one-hour, core-disabled transient worker, proved the restricted =18791 → 18792= bridge health path, created no button marker, and toggled the worker off. =xochitl= remained on PID =78929= with zero restarts, Smart and T.M.R. returned inactive, and =/= remained read-only.
- Guarded =refresh-functional= transaction =20260728T104334Z-63134= installed the exact visually approved historical stock-icon layout and wrote =validated:refresh-functional:78929=. Both canary units exited, the QMD remained byte-identical, and the installer touched neither boot state nor the root filesystem. This is not evidence of a physically completed functional button round trip.
- The Paper Pro 3.28.0.164 reinstall completed through a stock-rollback ReMagic
  canary followed by inert transaction =20260730T184327Z-34344= and functional
  transaction =20260730T184443Z-34618=. That historical legacy-v1 QMD was
  =2b9188af0c3fd726743e36ee1a3c86244cf6327ad22eeef1aa7a291a7add059d=;
  all eight intended QMDs and AppLoad mapped; =xochitl= remained on PID 9449
  with =NRestarts=0=; takeover and canary helpers were inactive; and =/= stayed
  read-only. Gestik's final live and protected settings both matched the
  standalone Mac preinstall hash at
  =826211118322c6a84d899a9cf2f11e3e24d5223ac11ee96efaf47e45a47f5938=.
  The repeatable package, Xovi, button, and rollback procedure is recorded in
  =../remarkable-beta-os/UPDATE-RECIPE.md=. Prior v2 transaction
  =20260731T162912Z-38343= superseded that legacy QMD; direct-launch inert and
  functional refreshes =20260731T222234Z-48219= and
  =20260731T222432Z-49869= now supersede the prior v2 QMD. Physical
  ink/image/mixed button acceptance remains steps 59 and 61.
- Guarded =refresh-inert= transaction =20260725T163235Z-44111= installed disabled two-button QMD SHA-256 =2d7777b0aaa0c50cee947117b42bf6d5cb03e13602dde4408046893c24949ca3= and received human visual confirmation.
- Guarded =refresh-functional= transaction =20260725T211315Z-12106= installed functional QMD SHA-256 =761b7fd4f86ceed9625a541c1a8e7c2c101abc835b56e22c8f8e1a5f919f8ac7=. Its qualification marker binds the exact device, firmware, QMD, and committed =xochitl= PID 60746. Watchdog/transaction units are inactive, no failure or rollback marker, deployment lock, or remote staging remains, =xochitl= has zero automatic restarts, Smart/Riddle workers are inactive, the server bridge/Gateway PIDs and restart counters are unchanged, and =/= remains read-only.

** Earlier one-button verification

- The selected handwritten question =What is 7+3= was detected as =Rect { x: 154, y: 83, w: 195, h: 45 }=.
- OpenAI returned an eight-line explanation in the first end-to-end test, proving selection and placement. That pen-stroke renderer was rejected for normal text because its traced glyphs looked poor.
- A synthetic no-draw API test loaded =selection_print.json= at runtime, called =draw_text=, and produced =7 + 3 = 10= without changing the validated binary.
- The final live test reused the same native marquee, invoked =draw_text= once, created an editable stock text object below the handwriting, and emitted no pen strokes.
- OpenClaw's loopback-only Chat Completions endpoint lists =openclaw/default= and passed authenticated plain-text smoke tests. The endpoint was not added to public nginx.
- The tablet's dedicated SSH key cannot open a shell and is authorized to forward only to =127.0.0.1:18789=. The launcher starts and removes that tunnel for each invocation.
- The active tablet =.env= contains only =OPENCLAW_GATEWAY_TOKEN=; the direct =OPENAI_API_KEY= and =OPENAI_BASE_URL= entries were removed.
- A dedicated =smart-remarkable-rmpp= OpenClaw session avoided contention with the default WhatsApp conversation. Its exact no-draw tablet route answered the stored arithmetic crop with =10= in eight seconds.
- The final live test detected the native marquee, sent only the selected handwriting to =openclaw/default=, received =10=, inserted it through the stock text tool, and exited successfully. Because this disposable page already contained the earlier test's text object in the placement area, reMarkable appended =10= to that existing editable object rather than creating another overlapping object.
- The OpenClaw watchdog now honors the gateway's plugin-heavy 90-second startup window instead of repeatedly restarting it before its port opens.
- The one-shot process exited, no Smart Remarkable or tunnel process remained, the trigger file was removed, =xochitl= remained active with zero restarts, and =/= remained read-only.
- All four Paper Pro regressions and all three OpenClaw transport tests pass. The relevant library suite is 17/17; one existing unrelated font-render test is excluded because it writes to the upstream author's missing hard-coded =/private/tmp/claude-501/...= directory.
- On the new Paper Pro 3.28.0.163, the revised relevant native suite is 36 passed, 0 failed, and 1 filtered; the only filtered case is that same upstream hard-coded font-render path. The installed three-mode aarch64 binary now has SHA-256 =7af19e4aa795180caab47afa8d8cffa1355badd596a28594032c3bfa6cf7d6ff= and requires no GLIBC newer than 2.28.
- The transactional installer placed the AppLoad bundle at =/home/root/xovi/exthome/appload/smart-remarkable=, verified the binary hash and mode-600 environment file, left =xochitl= active, and left =/= read-only. No boot unit, root-filesystem payload, kernel module, or injected =llmbutton= extension was installed.
- A display-free smoke test ran the installed binary against a synthetic handwritten request and the exact =agent:main:main= / =whatsapp= route. It received the requested =SMART_WHATSAPP_SHARED_OK= marker, retained no input/output file, and caused zero =xochitl= restarts.
- The installed AppLoad launcher started a transient worker and healthy forwarding-only tunnel; invoking it again cancelled both. =RuntimeMaxSec= remained ten minutes, core dumps remained disabled, the worker stayed bound to =xochitl= and conflicted with T.M.R., and the stock UI recorded zero restarts during this test.
- Xovi/AppLoad was restored with the existing supported start transaction after it was found installed but not mapped. The live stock sidebar visibly contained the AppLoad entry, =xochitl= logged that the external AppLoad hooks loaded, Wi-Fi key authentication remained available, and the root filesystem remained read-only.
- Do not use firmware 3.28.0.163's stock =/usr/bin/screenshot= helper for validation: it delivered =USR2= and caused one automatic =xochitl= restart instead of creating a PNG. Smart Remarkable's inherited read-only locator succeeded under one subsequent allocation order, but a later Riddle-driven restart exposed that it selected only the final =card0= group and could fail with =Invalid header length=.
- On the failing live layout, =xochitl= exposed three =card0= groups. Read-only header tracing rejected the first and last groups with zero-length headers and resolved the middle group through lengths =3526658=, =3526658=, and =14102530= to a completely readable 14,061,312-byte frame. No notebook data or process memory was changed.
- After another ordinary =xochitl= start, a read-only diagnostic found a second allocation shape: the remaining linked chain was =3526658=, =3526658=, then zero, and the unique terminal =14102530= header lived at offset =+8= in a separate anonymous =0xd73000=-byte mapping. The deployed first candidate fix rejected this unfamiliar shape before model submission or input injection, leaving the note untouched.
- A no-submit current-device smoke test sent an empty-selection trigger, captured read-only, returned =NoSelection=, and remained armed. This proves navigation or tool-selection pen taps no longer silently consume the one-shot worker.
- In the final current-device test, the user completed a stock lasso and lifted the pen without a second gesture. Smart ignored intermediate no-marquee candidates, detected =Rect { x: 464, y: 261, w: 147, h: 56 }=, sent only that crop through the canonical =agent:main:main= / =whatsapp= route, executed =draw_text=, inserted stock text, and exited.
- After the final round trip, =xochitl= remained active on the same PID with zero restarts, Smart/Riddle/tunnel processes were absent, trigger files were absent, no screenshot, output, or log was retained, and =/= remained read-only.
- AppLoad's filesystem scan root now contains exactly one Smart Remarkable directory and manifest. Three preserved rollback bundles live under root-only =/home/root/.smart-remarkable-recovery= outside that scan root; one **Reload** tap refreshes any stale in-memory AppLoad list.
- The new strict mode parser maps =once= to =pen-release --no-loop=, =session-hold= to =pen-hold=, and =session-auto= to looping =pen-release=. Its shell fixture suite covers all three modes and rejects invalid settings.
- The revised native library suite passes 36 tests with zero failures and one filtered upstream font-render test. It covers hold timing and jitter boundaries, tiny-contact rejection, timestamp regression, busy-at-pen-down rejection, one-owner admission, duplicate-selection policy, and the restriction that only a captured =LlmButton= selection can request stock dismissal.
- The inert and functional QMLDiff artifacts both pass offline compatibility and apply-diff checks against the exact extracted 3.28.0.163 =SceneSelectionHandler.qml= tree. The functional patch contains only a stock-style =LLM= button and a dynamic AppLoad =--selection-button= request; it contains no credential, network, provider, Draw, or native-ABI implementation.
- The three-mode bundle is installed with =session-hold= as the default. A no-request session smoke test verified its readiness marker, one-hour transient bound, clean stop, inactive T.M.R., unchanged =xochitl= PID, zero restarts, and read-only root.
- The first inert-button transaction failed closed at its pre-arm fingerprint because the live Xovi drop-in directory also contains generated =extensions.d= and =exthome= symlinks. It installed no QMD, did not restart =xochitl=, and left the root read-only. The predicate was corrected to require those exact root-owned links and destinations, then passed two fresh independent reviews.
- Inert transaction =20260724T150700Z-68965= committed the disabled QMD with SHA-256 =8fa36651d350eaffc19890730362e2bc7437e21848f9d27397205c8b01cc168a=. The watchdog validated =xochitl= PID =36281=, both transient units exited, the deployment lock was removed, =xochitl= stayed active with zero automatic restarts, and =/= remained read-only.
- The user confirmed the disabled =LLM= item was visible and the menu remained stable. Tablet logs showed no trigger or request, while the canonical OpenClaw session and gateway logs showed no tablet Chat Completions turn; the inert item therefore behaved as designed.
- Functional transaction =20260724T155040Z-86105= promoted the exact confirmed location to QMD SHA-256 =b897962d221e552e8fa74c5b7b99373b0a04c8582f06b0da70390a74f1ca15f6=. The watchdog validated =xochitl= PID =40231=, both transient units and the deployment lock cleared, Smart and T.M.R. remained inactive, =xochitl= stayed active with zero automatic restarts, and =/= remained read-only. Step 28 remains open for live button and fallback acceptance.
- The first live tap on that functional button was accepted once at 2026-07-24 15:57 UTC. Smart detected =Rect { x: 235, y: 443, w: 228, h: 188 }=, ignored repeated taps while busy, and completed =draw_text=. The private gateway recorded exactly one canonical =agent:main:main= / =whatsapp= API-style turn, with no duplicate or ordinary WhatsApp traffic in the checked window; =xochitl= stayed on PID =40231= with zero restarts and =/= stayed read-only.
- The after-Copy feedback patch applies cleanly to the exact extracted 3.28.0.163 resource tree as =Cut → Copy → LLM → Convert to text → Delete=. Its QMD SHA-256 is =0aaef0c1d75cdaf5634fc490cae3659f0d764ca4f4d0911eea4ceb0c7535b6c8=; the matching disabled canary is =a53f8b9f2e33867fdc86ec7c41e0f107c99a6004db1d1554243b03cfa3c628c7=. The installed aarch64 worker is =7af19e4aa795180caab47afa8d8cffa1355badd596a28594032c3bfa6cf7d6ff= and requires no GLIBC symbol newer than 2.28.
- The revised worker binary was transactionally installed under =/home= with SHA-256 =7af19e4aa795180caab47afa8d8cffa1355badd596a28594032c3bfa6cf7d6ff=. Refresh-inert transaction =20260724T163819Z-18257= then committed the new disabled after-Copy QMD =a53f8b9f2e33867fdc86ec7c41e0f107c99a6004db1d1554243b03cfa3c628c7=; the watchdog validated =xochitl= PID =46307=, zero restarts, cleared both units and the deployment lock, kept all assistants inactive, and left =/= read-only.
- After an ordinary restart left =xochitl= in stock mode, a read-only diagnosis confirmed that Xovi was simply not mapped; the QMD had not damaged or destabilized the stock application. Xovi was reactivated through a bounded stock-rollback watchdog. The exact-device coexistence policy then required =penLayerMemory-beta-3.28.0.163.qmd= at SHA-256 =9862cb5a2a4e0df90ee7a94c4013a5dbc7e2634fd2e2b16bbb48842c1154ebce=; =quickSettingsTimer-beta-3.28.0.163.qmd= was absent and remained optional only at its reviewed SHA-256 =01514a3d969189d7cac9e4d65584c3d6b7674fcfe9565f066495eb0847910016=. Two independent adversarial reviews passed the revised enumeration and required-key logic.
- Refresh-functional transaction =20260724T173524Z-61264= committed the after-Copy functional QMD at SHA-256 =0aaef0c1d75cdaf5634fc490cae3659f0d764ca4f4d0911eea4ceb0c7535b6c8=. Two delayed postchecks validated =xochitl= PID =5247= with zero restarts, mapped Xovi, QRR, and AppLoad, the exact worker and co-resident Pen Layer Memory hashes, inactive Smart/Riddle services and canary units, no deployment lock, and a read-only root. Interactive no-submit and live-tap acceptance of the revised feedback behavior remains step 30; no notebook was modified merely to manufacture that test.

* Later

- Consider upstreaming the trigger-source fix after device validation.
- Consider a user-friendly Mac shortcut for the one-shot SSH command.
- Detect an already-occupied stock text object in the automatic placement area and choose the next free vertical position.
- Treat every future firmware version as unsupported until a new exact resource extraction, compatibility allowlist, inert canary, and rollback validation are completed.
